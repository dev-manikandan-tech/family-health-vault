# packages/ui

Shared UI components built on React Native for Web and/or Tamagui so they can be used in `apps/web`, `apps/admin`, and `apps/mobile`.

## Contents

- Design tokens (colors, typography, spacing)
- Primitive components (Button, Input, Card, Avatar)
- Composites (DocumentCard, TimelineItem, VisitRow)
- Accessibility wrappers