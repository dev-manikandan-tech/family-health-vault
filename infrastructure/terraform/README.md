# infrastructure/terraform

Terraform modules and environment roots for GCP infrastructure.

## Layout

- `modules/` — reusable VPC, GKE, Cloud SQL, Redis, Storage, IAM, DNS modules
- `environments/dev/`
- `environments/staging/`
- `environments/prod/`