# infrastructure/kubernetes

Kubernetes manifests and Helm charts for deploying services to GKE.

## Layout

- `base/` — base Kustomize manifests
- `overlays/` — environment-specific overlays
- `helm/` — Helm chart for the full stack